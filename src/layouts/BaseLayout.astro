---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = 'Cowork SG — Practical AI workflows for non-technical professionals. Learn to use AI tools like Claude, ChatGPT, and Gemini to build repeatable systems without coding.',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalURL} />
    <link rel="alternate" type="application/rss+xml" title="Cowork SG Blog" href="/rss.xml" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta name="robots" content="index, follow" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400;1,600;1,700&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/@fontsource/dm-sans/index.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/@fontsource/dm-sans/800.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/@fontsource/outfit/400.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/@fontsource/dm-mono/500.css" rel="stylesheet" />

    <!-- Prevent dark mode flash -->
    <script is:inline>
      const t = localStorage.getItem('theme');
      if (t === 'dark' || (!t && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body class="bg-salt dark:bg-navy text-navy dark:text-salt font-sans transition-colors duration-200 min-h-screen flex flex-col">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style is:global>
  /* Graph-paper grid texture — dark sections */
  .grid-bg {
    background-image:
      linear-gradient(rgba(196, 166, 152, 0.07) 1px, transparent 1px),
      linear-gradient(90deg, rgba(196, 166, 152, 0.07) 1px, transparent 1px);
    background-size: 28px 28px;
  }
  /* Subtle grid for light sections */
  .grid-bg-light {
    background-image:
      linear-gradient(rgba(27, 52, 100, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(27, 52, 100, 0.04) 1px, transparent 1px);
    background-size: 28px 28px;
  }
  /* Prose overrides */
  .prose :where(blockquote) {
    border-left-color: #C84330;
    font-family: 'Playfair Display', serif;
    font-style: italic;
    font-size: 1.15em;
    color: #1B3464;
  }
  .dark .prose :where(blockquote) {
    color: #F5DDD6;
  }
  .prose :where(a) {
    color: #C84330;
    text-decoration-color: rgba(200, 67, 48, 0.4);
  }
  .prose :where(code):not(:where(pre code)) {
    background: rgba(245, 221, 214, 0.4);
    color: #1B3464;
    padding: 1px 5px;
    border-radius: 3px;
    font-size: 0.875em;
  }
  .dark .prose :where(code):not(:where(pre code)) {
    background: rgba(245, 221, 214, 0.12);
    color: #F5DDD6;
  }
</style>
